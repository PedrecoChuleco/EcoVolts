
    import * as React from 'react';

    import { IconType } from '../types';

    type SiTyProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#46EBE1';

    const SiTy: IconType = React.forwardRef<SVGSVGElement, SiTyProps>(function SiTy({title = 'ty', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M24 3.84H13.92V0h-12v3.84H0v9.12h1.92v7.147A3.893 3.893 0 0 0 5.813 24H24v-9.12H13.92v-1.92h6.187A3.893 3.893 0 0 0 24 9.067Z' />
        </svg>
      );
    });

    export { SiTy as default, defaultColor };
  