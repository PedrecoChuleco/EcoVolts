
    import * as React from 'react';

    import { IconType } from '../types';

    type SiZxProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#F11A7B';

    const SiZx: IconType = React.forwardRef<SVGSVGElement, SiZxProps>(function SiZx({title = 'zx', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M22.036 18.327v5.673h-20.072v-5.673l6.152-6.56L2.4 5.673V0h19.156v5.673l-5.716 6.094 6.153 6.56h0.043Zm-10.058-10.677l1.855-1.977h-3.709l1.854 1.977Zm0 8.235l-2.291 2.442h4.582l-2.291-2.442Z' />
        </svg>
      );
    });

    export { SiZx as default, defaultColor };
  