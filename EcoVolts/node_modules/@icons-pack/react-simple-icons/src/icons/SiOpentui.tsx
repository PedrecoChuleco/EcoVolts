
    import * as React from 'react';

    import { IconType } from '../types';

    type SiOpentuiProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#000000';

    const SiOpentui: IconType = React.forwardRef<SVGSVGElement, SiOpentuiProps>(function SiOpentui({title = 'OpenTUI', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M0 1.2v21.6h24V1.2Zm4.8 6h3.6v9.6H4.8Z' />
        </svg>
      );
    });

    export { SiOpentui as default, defaultColor };
  