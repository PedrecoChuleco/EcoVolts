
    import * as React from 'react';

    import { IconType } from '../types';

    type SiPiProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#000000';

    const SiPi: IconType = React.forwardRef<SVGSVGElement, SiPiProps>(function SiPi({title = 'Pi', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M0 0v24h6v-6h6v-6H6V6h6v6h6V0Zm18 12v12h6V12Z' />
        </svg>
      );
    });

    export { SiPi as default, defaultColor };
  