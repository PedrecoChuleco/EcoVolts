
    import * as React from 'react';

    import { IconType } from '../types';

    type SiOpencodeProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#000000';

    const SiOpencode: IconType = React.forwardRef<SVGSVGElement, SiOpencodeProps>(function SiOpencode({title = 'OpenCode', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M22 24H2V0h20zM17 4.8H7v14.4h10z' />
        </svg>
      );
    });

    export { SiOpencode as default, defaultColor };
  