
    import * as React from 'react';

    import { IconType } from '../types';

    type SiGlassProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#FFCC00';

    const SiGlass: IconType = React.forwardRef<SVGSVGElement, SiGlassProps>(function SiGlass({title = 'Glass', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M11 0v24A12 12 0 0 1 0 12 12 12 0 0 1 11 0m13 13a11 11 0 0 1-11 11V13zm-5.5-2a1 1 0 0 0 0-11 1 1 0 0 0 0 11' />
        </svg>
      );
    });

    export { SiGlass as default, defaultColor };
  