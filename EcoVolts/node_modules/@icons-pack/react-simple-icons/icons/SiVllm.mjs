import { jsxs, jsx } from 'react/jsx-runtime';
import * as React from 'react';

const defaultColor = "#30A2FF";
const SiVllm = React.forwardRef(function SiVllm2({ title = "vLLM", color = "currentColor", size = 24, ...others }, ref) {
  if (color === "default") {
    color = defaultColor;
  }
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: size,
      height: size,
      fill: color,
      viewBox: "0 0 24 24",
      ref,
      ...others,
      children: [
        /* @__PURE__ */ jsx("title", { children: title }),
        /* @__PURE__ */ jsx("path", { d: "m23.6 0-8.721 4.59L9.829 24h7.41zM9.83 24V5.142H.4Z" })
      ]
    }
  );
});

export { SiVllm as default, defaultColor };
